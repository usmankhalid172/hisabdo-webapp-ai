# Core assistant runs on the Python standard library only (offline, no API key).
# Optional dependencies (only for API server + tests):
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0

# Development / test extras (tests also run with stdlib unittest, no extra install):
# httpx is required by fastapi.testclient.TestClient
httpx>=0.24